{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RacerPause</h1>

    <table class="record_properties table table-condensed table-stripped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Porder</th>
                <td>{{ entity.porder }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('racerpause') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('racerpause_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
