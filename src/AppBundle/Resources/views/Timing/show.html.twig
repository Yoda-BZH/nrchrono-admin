{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Timing</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Timing</th>
                <td>{{ entity.timing }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Isrelay</th>
                <td>{{ entity.isRelay }}</td>
            </tr>
            <tr>
                <th>IdRacer</th>
                <td>{{ entity.idRacer.nickname }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('timing') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('timing_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
