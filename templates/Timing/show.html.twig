{% extends 'base.html.twig' %}

{% block body -%}
    <h1>{% trans %}Timing{% endtrans %}</h1>

{% include "Timing/item.html.twig" with { 'entity': entity } %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('timing') }}">
            {% trans %}Back to the list{% endtrans %}
        </a>
    </li>
    <li>
        <a href="{{ path('timing_edit', { 'id': entity.id }) }}">
            {% trans %}Edit{% endtrans %}
        </a>
    </li>
</ul>
    {{ include('Timing/_delete_form.html.twig') }}
{% endblock %}
