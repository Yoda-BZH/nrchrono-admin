{% extends 'base.html.twig' %}

{% block body -%}
    <h1>{% trans %}Ranking creation{% endtrans %}</h1>

    {{ include('Ranking/_form.html.twig') }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('ranking') }}">
            {% trans %}Back to the list{% endtrans %}
        </a>
    </li>
</ul>
{% endblock %}
