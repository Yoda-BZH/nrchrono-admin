{% extends 'base.html.twig' %}

{% block title %}{% trans %}RacerPause{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}RacerPause{% endtrans %}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{% trans %}Id{% endtrans %}</th>
                <td>{{ racer_pause.id }}</td>
            </tr>
            <tr>
                <th>{% trans %}Porder{% endtrans %}</th>
                <td>{{ racer_pause.porder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('racerpause') }}">{% trans %}back to list{% endtrans %}</a>

    <a href="{{ path('racer_pause_edit', {'id': racer_pause.id}) }}">{% trans %}edit{% endtrans %}</a>

    {{ include('racer_pause/_delete_form.html.twig') }}
{% endblock %}
