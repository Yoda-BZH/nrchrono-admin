{% extends 'base.html.twig' %}

{% block title %}RacerPause{% endblock %}

{% block body %}
    <h1>RacerPause</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ racer_pause.id }}</td>
            </tr>
            <tr>
                <th>Porder</th>
                <td>{{ racer_pause.porder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('racerpause') }}">back to list</a>

    <a href="{{ path('racer_pause_edit', {'id': racer_pause.id}) }}">edit</a>

    {{ include('racer_pause/_delete_form.html.twig') }}
{% endblock %}
