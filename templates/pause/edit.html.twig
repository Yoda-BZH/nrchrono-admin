{% extends 'base.html.twig' %}

{% block title %}Edit Pause{% endblock %}

{% block body %}
    <h1>{% trans %}Edit Pause{% endtrans %}</h1>

    {{ include('pause/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('pause') }}">{% trans %}back to list{% endtrans %}</a>

    {{ include('pause/_delete_form.html.twig') }}
{% endblock %}
