{% extends 'base.html.twig' %}

{% block title %}Pause{% endblock %}

{% block body %}
    <h1>Pause</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pause.id }}</td>
            </tr>
            <tr>
                <th>Porder</th>
                <td>{{ pause.porder }}</td>
            </tr>
            <tr>
                <th>HourStart</th>
                <td>{{ pause.hourStart ? pause.hourStart|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>HourStop</th>
                <td>{{ pause.hourStop ? pause.hourStop|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pause_index') }}">back to list</a>

    <a href="{{ path('pause_edit', {'id': pause.id}) }}">edit</a>

    {{ include('pause/_delete_form.html.twig') }}
{% endblock %}
