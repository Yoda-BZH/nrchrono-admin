<form method="post" action="{{ path('racerpause_delete', {'id': racerpause.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ racerpause.id) }}">
    <button class="btn">Delete</button>
</form>
