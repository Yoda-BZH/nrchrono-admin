{% extends 'base.html.twig' %}

{% block body -%}
    <h1>RacerPause edit</h1>


    {{ include('RacerPause/_form.html.twig', {'button_label': 'Update'}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('racerpause') }}">
            Back to the list
        </a>
    </li>
    {{ include('RacerPause/_delete_form.html.twig') }}
</ul>
{% endblock %}
