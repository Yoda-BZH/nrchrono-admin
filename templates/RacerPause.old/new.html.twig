{% extends 'base.html.twig' %}

{% block body -%}
    <h1>RacerPause creation</h1>

    {{ include('RacerPause/_form.html.twig') }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('racerpause') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
